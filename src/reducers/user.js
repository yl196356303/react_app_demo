const initState = {
    id: '',
    displayName: '',
    avatar: '',
    role: '',
    isLogin: false
}

export default (state = initState, action) => {
    switch (action.type) {
        default:
            return state
    }
}